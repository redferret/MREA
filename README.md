To setup the application run the setup script. Make sure the file
can be run
```
$ ./setup.sh "f7fy8gfdsuyg" "mrea-plan" 
```

To update the project run the following
```
$ ./update.sh "mrea-plan"
```
